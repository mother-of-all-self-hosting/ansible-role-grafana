# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Check existence of Grafana systemd service
  ansible.builtin.stat:
    path: "{{ devture_systemd_docker_base_systemd_path }}/{{ grafana_identifier }}.service"
  register: grafana_service_stat

- name: Uninstall Grafana systemd services and files
  when: grafana_service_stat.stat.exists | bool
  block:
    - name: Ensure Grafana systemd service is stopped
      ansible.builtin.service:
        name: "{{ grafana_identifier }}"
        state: stopped
        enabled: false
        daemon_reload: true

    - name: Ensure Grafana systemd service does not exist
      ansible.builtin.file:
        path: "{{ devture_systemd_docker_base_systemd_path }}/{{ grafana_identifier }}.service"
        state: absent

    - name: Ensure Grafana container network does not exist
      when: grafana_container_network_deletion_enabled
      block:
        - name: Ensure Grafana container network does not exist via community.docker.docker_network
          when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
          community.docker.docker_network:
            name: "{{ grafana_container_network }}"
            state: absent
          register: grafana_network_deletion_result

        - name: Ensure Grafana container network does not exist via ansible.builtin.command
          when: devture_systemd_docker_base_container_network_creation_method == 'command'
          ansible.builtin.command:
            cmd: >-
              {{ devture_systemd_docker_base_host_command_docker }} network rm
              {{ grafana_container_network }}
          register: grafana_network_deletion_result
          changed_when: grafana_container_network in grafana_network_deletion_result.stdout

    - name: Ensure Grafana path does not exist
      ansible.builtin.file:
        path: "{{ grafana_base_path }}"
        state: absent
